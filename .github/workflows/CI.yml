name: CI Pipeline - Angular

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install Dependencies
        run: npm ci --prefix exampleCICD

      - name: Lint Code
        run: npm run lint --prefix exampleCICD

      - name: Run Unit Tests
        run: npm run test --prefix exampleCICD -- --watch=false --browsers=ChromeHeadless

      - name: Build Angular App
        run: npm run build --prefix exampleCICD -- --configuration production

      - name: Upload Build Artifact
        uses: actions/upload-artifact@v4
        with:
          name: angular-dist
          path: exampleCICD/dist/
